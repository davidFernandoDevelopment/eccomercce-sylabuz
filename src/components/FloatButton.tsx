import React from 'react';

const FloatButton = () => {

    return (
        <button className='c-floatButton'>
            <span className='c-floatButton__icon'>+</span>
        </button>
    );
};

export default FloatButton;